import{_ as t,c as a,o as r,ag as n}from"./chunks/framework.B7Jkh64R.js";const u=JSON.parse('{"title":"Why WebUI?","description":"","frontmatter":{},"headers":[],"relativePath":"guide/why.md","filePath":"guide/why.md"}'),s={name:"guide/why.md"};function i(o,e,l,d,c,p){return r(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="why-webui" tabindex="-1">Why WebUI? <a class="header-anchor" href="#why-webui" aria-label="Permalink to &quot;Why WebUI?&quot;">​</a></h1><h2 id="the-problem" tabindex="-1">The Problem <a class="header-anchor" href="#the-problem" aria-label="Permalink to &quot;The Problem&quot;">​</a></h2><p>Server-side rendering has two costs that compound at scale.</p><p><strong>On the server</strong>, traditional SSR frameworks re-parse templates on every request - tokenizing, building an AST, evaluating expressions, and serializing output. This work is redundant: the template structure hasn&#39;t changed since the last request, only the data has. JavaScript-based SSR (Next.js, Nuxt, Remix) adds a second layer of overhead by requiring a Node.js runtime - with garbage collection pauses, JIT warmup costs, and memory pressure that grow worse under load.</p><p><strong>On the client</strong>, modern frameworks ship entire component trees as JavaScript bundles. The pipeline is sequential and blocking: download the JS bundle → parse it → compile it → execute it → fetch data → render. Nothing paints until the bundle finishes. On constrained devices - 4 GB of RAM, older mobile CPUs - this pipeline takes 1–2 seconds. Every component in the tree ships JavaScript, even components that never handle user interaction and never update.</p><p>Both approaches do redundant work on every request, ship unnecessary code to the browser, and scale poorly under load.</p><h2 id="the-insight" tabindex="-1">The Insight <a class="header-anchor" href="#the-insight" aria-label="Permalink to &quot;The Insight&quot;">​</a></h2><p>HTML template structure is <strong>static</strong>. It does not change between requests - only the data changes. This is the same insight that separates compiled languages from interpreted ones: move the expensive work (parsing, analysis, optimization) to build time, and keep runtime costs minimal.</p><p>WebUI applies this principle to web rendering. Templates are compiled <strong>once</strong> into a compact Protocol Buffer binary. At runtime, a handler reads the protocol sequentially - emitting static fragments as-is and resolving dynamic fragments from a state object. There is no parsing, no AST walking, no expression compilation at request time.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>HTML + CSS templates → webui build → protocol.bin → handler (any lang) + state → rendered HTML</span></span></code></pre></div><h2 id="the-web-platform-bet" tabindex="-1">The Web Platform Bet <a class="header-anchor" href="#the-web-platform-bet" aria-label="Permalink to &quot;The Web Platform Bet&quot;">​</a></h2><p>Historically, frameworks existed because the web platform lacked key primitives. That is no longer the case. Modern browsers ship with:</p><ul><li><strong>Web Components</strong> - reusable custom elements with a standard lifecycle, no framework runtime required</li><li><strong>Declarative Shadow DOM</strong> - server-renderable encapsulation without JavaScript</li><li><strong>CSS containment</strong> - layout and paint isolation for predictable rendering performance</li><li><strong>Adopted stylesheets</strong> - shared, constructable stylesheets across shadow roots</li><li><strong>Navigation API</strong> - client-side routing without framework abstractions</li></ul><p>WebUI builds directly on these platform primitives rather than wrapping them in an abstraction layer. Templates use standard HTML and native Web Components. The optional client-side router uses the Navigation API. Styling uses adopted stylesheets and CSS containment. No proprietary component model, no virtual DOM, no framework runtime in the browser.</p><p>When you build on the web platform, you inherit its improvements for free. Every browser performance optimization, every new CSS feature, every platform API lands in your app without a framework upgrade.</p><h2 id="islands-architecture" tabindex="-1">Islands Architecture <a class="header-anchor" href="#islands-architecture" aria-label="Permalink to &quot;Islands Architecture&quot;">​</a></h2><p>WebUI uses an <strong>Islands Architecture</strong> where each Web Component is an independent island of interactivity.</p><p>The idea is simple: most of a web page is static content - headings, paragraphs, images, layout. Only specific parts of the page need to respond to user interaction - buttons, forms, search boxes, interactive widgets. Why ship JavaScript for all of it?</p><p>With WebUI&#39;s Islands Architecture:</p><ul><li><p><strong>Static content</strong> is server-rendered HTML. It arrives fully formed in the initial response. No JavaScript is shipped, no hydration occurs, no client-side processing is needed. It is just HTML and CSS - the fastest thing a browser can render.</p></li><li><p><strong>Interactive components</strong> are Web Components that hydrate on the client. Each island is self-contained with its own Shadow DOM, encapsulated styles, and TypeScript behavior. Islands hydrate independently - they don&#39;t wait for each other or for a global framework to initialize.</p></li><li><p><strong>You control the boundary.</strong> The hydration plugin system lets you decide exactly which components are interactive islands and how they hydrate (on load, on visible, on interaction).</p></li></ul><h3 id="the-practical-difference" tabindex="-1">The practical difference <a class="header-anchor" href="#the-practical-difference" aria-label="Permalink to &quot;The practical difference&quot;">​</a></h3><p>Consider a product page with 10 components: a header, navigation, breadcrumbs, product title, product image, description, price display, &quot;Add to Cart&quot; button, reviews list, and a review form.</p><table tabindex="0"><thead><tr><th>Approach</th><th>JavaScript shipped</th></tr></thead><tbody><tr><td>Traditional SPA</td><td>All 10 components ship JS, hydrate, and re-render on the client</td></tr><tr><td>WebUI Islands</td><td>Only 2 components ship JS - the &quot;Add to Cart&quot; button and the review form. The other 8 are static HTML with zero client-side cost</td></tr></tbody></table><p>The result: dramatically less JavaScript, faster time-to-interactive, and better performance on constrained devices.</p><h2 id="declarative-templates" tabindex="-1">Declarative Templates <a class="header-anchor" href="#declarative-templates" aria-label="Permalink to &quot;Declarative Templates&quot;">​</a></h2><p>WebUI enforces a strict separation of concerns:</p><ul><li><strong>HTML</strong> for structure - clean, semantic markup with template directives (<code>&lt;if&gt;</code>, <code>&lt;for&gt;</code>, <code>{{}}</code>)</li><li><strong>CSS</strong> for styling - standard stylesheets with Shadow DOM encapsulation</li><li><strong>TypeScript</strong> for behavior - only in interactive island components</li></ul><p>Each concern lives in its own file. There is no JSX that mixes markup and logic. No template literals that embed HTML in JavaScript. No CSS-in-JS that couples styling to runtime execution.</p><p>This separation is intentional and performance-motivated:</p><ol><li><strong>HTML and CSS are the fastest things a browser can parse.</strong> Keeping them pure means the browser&#39;s optimized parsing paths handle them directly, with no framework interpretation layer.</li><li><strong>Static templates compile better.</strong> Because structure and logic are separated, the build step can analyze, optimize, and pre-serialize the static parts more aggressively.</li><li><strong>TypeScript only ships where needed.</strong> Because behavior is in separate files attached only to interactive islands, JavaScript is never bundled for static components.</li></ol><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>my-component/</span></span>
<span class="line"><span>├── my-component.html   ← structure</span></span>
<span class="line"><span>├── my-component.css    ← styling</span></span>
<span class="line"><span>└── my-component.ts     ← behavior (only for interactive islands)</span></span></code></pre></div><h2 id="language-agnostic" tabindex="-1">Language Agnostic <a class="header-anchor" href="#language-agnostic" aria-label="Permalink to &quot;Language Agnostic&quot;">​</a></h2><p>The compiled protocol is a binary format - just bytes. Any language that can read bytes and write strings can render WebUI templates. Native handler implementations exist for:</p><table tabindex="0"><thead><tr><th>Language</th><th>Integration</th></tr></thead><tbody><tr><td><strong>Rust</strong></td><td>Native crate (<code>webui-handler</code>)</td></tr><tr><td><strong>Node.js</strong></td><td>Native addon (also works with Bun and Deno)</td></tr><tr><td><strong>C# / .NET</strong></td><td>Native bindings</td></tr><tr><td><strong>Go</strong></td><td>C FFI bindings</td></tr><tr><td><strong>Python</strong></td><td>C FFI bindings</td></tr><tr><td><strong>Any other language</strong></td><td>C FFI interface</td></tr></tbody></table><p>A Go service, a Rust microservice, and a C# API can all render the same compiled templates using the same protocol. Your frontend team writes templates once; every backend team consumes them.</p><h2 id="performance" tabindex="-1">Performance <a class="header-anchor" href="#performance" aria-label="Permalink to &quot;Performance&quot;">​</a></h2><p>Build-time compilation eliminates per-request template overhead. Islands Architecture eliminates unnecessary client-side JavaScript. Together, they produce measurable gains:</p><table tabindex="0"><thead><tr><th>Benchmark</th><th>Result</th></tr></thead><tbody><tr><td>vs. Fastify (raw SSR)</td><td><strong>4.3× faster</strong></td></tr><tr><td>vs. React SSR</td><td><strong>8.2× faster</strong></td></tr><tr><td>Small pages</td><td><strong>Sub-millisecond</strong> rendering</td></tr><tr><td>Large lists (1,000+ items)</td><td><strong>Linear scaling</strong></td></tr></tbody></table><p>These numbers follow directly from the architecture:</p><ul><li><strong>Static fragments</strong> are pre-serialized bytes copied directly to the output buffer. No string concatenation, no template interpretation.</li><li><strong>Dynamic fragments</strong> resolve to simple key lookups against a flat state object. No expression compilation at runtime.</li><li><strong>No runtime allocations</strong> for template structure - the protocol binary is read sequentially, and output is written to a pre-allocated buffer.</li><li><strong>No client-side framework runtime</strong> - islands hydrate independently using native Web Component lifecycle hooks, not a framework reconciliation pass.</li></ul><h2 id="no-node-js-required" tabindex="-1">No Node.js Required <a class="header-anchor" href="#no-node-js-required" aria-label="Permalink to &quot;No Node.js Required&quot;">​</a></h2><p>This is not a minor implementation detail - it is a fundamental architectural advantage.</p><p>Traditional SSR frameworks require a Node.js process on every server. That means:</p><ul><li><strong>V8 engine overhead</strong> - garbage collection pauses cause latency spikes under load</li><li><strong>JIT warmup</strong> - the first requests are slow while V8 compiles hot paths</li><li><strong>High memory baseline</strong> - a Node.js process starts at ~30–50 MB before your application loads</li><li><strong>Single-threaded event loop</strong> - CPU-bound rendering blocks all other requests</li></ul><p>WebUI&#39;s Rust-native handler eliminates all of this:</p><ul><li><strong>No garbage collector</strong> - deterministic memory management, no pauses</li><li><strong>No JIT warmup</strong> - compiled ahead of time, first request is as fast as the millionth</li><li><strong>Minimal memory footprint</strong> - the handler is a small, statically-linked binary</li><li><strong>Multi-threaded</strong> - handles concurrent requests across all CPU cores without contention</li></ul><p>The practical result: fewer servers, lower memory consumption, more predictable latency, and lower cloud bills. A single WebUI server can handle the load that previously required multiple Node.js instances behind a load balancer.</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>WebUI exists because modern web rendering does too much redundant work - on the server and in the browser.</p><table tabindex="0"><thead><tr><th>Problem</th><th>WebUI&#39;s Answer</th></tr></thead><tbody><tr><td>Templates re-parsed every request</td><td>Compiled once to binary protocol at build time</td></tr><tr><td>Full JS bundles shipped to browser</td><td>Islands Architecture - only interactive components ship JS</td></tr><tr><td>Framework abstractions over the platform</td><td>Direct use of Web Components, Shadow DOM, Navigation API</td></tr><tr><td>JSX / CSS-in-JS mixing concerns</td><td>Declarative separation - HTML, CSS, TypeScript in separate files</td></tr><tr><td>Node.js runtime required on server</td><td>Rust-native rendering, no JavaScript runtime, no GC pauses</td></tr><tr><td>Single-language lock-in</td><td>Language-agnostic protocol - render from Rust, Node, Go, C#, Python</td></tr></tbody></table><p>The result is a framework that is extremely fast, renders pages in <strong>sub-millisecond time</strong>, ships <strong>minimal JavaScript to the browser</strong>, and works from <strong>any backend language</strong> - without a JavaScript runtime on the server.</p>`,51)])])}const m=t(s,[["render",i]]);export{u as __pageData,m as default};

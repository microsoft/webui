import{_ as a,c as t,o as s,ag as i}from"./chunks/framework.IQH0tUiJ.js";const u=JSON.parse('{"title":"WebUI Electron Handler","description":"","frontmatter":{},"headers":[],"relativePath":"guide/concepts/handlers/electron.md","filePath":"guide/concepts/handlers/electron.md"}'),n={name:"guide/concepts/handlers/electron.md"};function o(r,e,l,d,c,p){return s(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="webui-electron-handler" tabindex="-1">WebUI Electron Handler <a class="header-anchor" href="#webui-electron-handler" aria-label="Permalink to &quot;WebUI Electron Handler&quot;">​</a></h1><p>WebUI apps can run as native desktop applications using Electron. The Electron integration uses the <code>webui-node</code> native addon to render pre-built protocols at startup, then serves the rendered HTML and static assets through a custom <code>webui://</code> protocol scheme — no HTTP server needed.</p><h2 id="how-it-works" tabindex="-1">How it Works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it Works&quot;">​</a></h2><ol><li><strong>Build phase</strong> — <code>webui build --plugin=fast</code> compiles templates into <code>protocol.bin</code>. esbuild bundles the client JS.</li><li><strong>Startup</strong> — Electron&#39;s main process loads the native addon (<code>webui-node</code>), reads <code>protocol.bin</code> + <code>state.json</code>, and calls <code>addon.render()</code> to produce the full SSR HTML.</li><li><strong>Custom protocol</strong> — A <code>webui://</code> protocol scheme is registered. When Electron loads <code>webui://app/</code>, it serves the rendered HTML. CSS and JS assets are served from the app&#39;s <code>dist/</code> directory.</li><li><strong>Hydration</strong> — The client JS bundle hydrates the SSR output, attaching event listeners and enabling interactivity — same as in a browser.</li></ol><h2 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 1. Build the native addon</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cargo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> microsoft-webui-node</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --release</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 2. Build a WebUI app (e.g., contact-book-manager)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/app/contact-book-manager</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 3. Run it in Electron</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/integration/electron</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> electron</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dist/main.js</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ../../app/contact-book-manager/dist</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ../../app/contact-book-manager/data/state.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --plugin=fast</span></span></code></pre></div><h2 id="cli-arguments" tabindex="-1">CLI Arguments <a class="header-anchor" href="#cli-arguments" aria-label="Permalink to &quot;CLI Arguments&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Argument</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>dist-dir</code></td><td>Path to app&#39;s <code>dist/</code> directory with <code>protocol.bin</code> and assets</td><td><code>../../app/hello-world/dist</code></td></tr><tr><td><code>state.json</code></td><td>Path to state JSON file</td><td><code>../../app/hello-world/data/state.json</code></td></tr><tr><td><code>--plugin=fast</code></td><td>Enable FAST hydration plugin</td><td>None</td></tr></tbody></table><h2 id="custom-titlebar" tabindex="-1">Custom Titlebar <a class="header-anchor" href="#custom-titlebar" aria-label="Permalink to &quot;Custom Titlebar&quot;">​</a></h2><p>The integration uses <code>titleBarStyle: &#39;hidden&#39;</code> with <code>titleBarOverlay</code> for a frameless native look. The app&#39;s header component uses <code>-webkit-app-region: drag</code> to act as the drag handle. Interactive elements within the header use <code>-webkit-app-region: no-drag</code> to remain clickable.</p><h2 id="the-webui-protocol" tabindex="-1">The <code>webui://</code> Protocol <a class="header-anchor" href="#the-webui-protocol" aria-label="Permalink to &quot;The \`webui://\` Protocol&quot;">​</a></h2><p>Electron&#39;s custom protocol handler maps routes to content:</p><ul><li><code>webui://app/</code> → SSR-rendered HTML</li><li><code>webui://app/*.css</code> → CSS files from the dist directory</li><li><code>webui://app/*.js</code> → JS bundles from the dist directory</li></ul><p>This avoids the need for a local HTTP server and provides a clean, secure origin for the app.</p><h2 id="example" tabindex="-1">Example <a class="header-anchor" href="#example" aria-label="Permalink to &quot;Example&quot;">​</a></h2><p>A complete working example is available at <a href="https://github.com/user/webui/tree/main/examples/integration/electron" target="_blank" rel="noreferrer"><code>examples/integration/electron/</code></a>.</p><p>The <a href="https://github.com/user/webui/tree/main/examples/app/contact-book-manager" target="_blank" rel="noreferrer">Contact Book Manager</a> app demonstrates a full-featured WebUI application that works both in the browser (via <code>webui serve</code>) and as an Electron desktop app.</p><h2 id="performance-notes" tabindex="-1">Performance Notes <a class="header-anchor" href="#performance-notes" aria-label="Permalink to &quot;Performance Notes&quot;">​</a></h2><ul><li>The native addon renders the entire page synchronously at startup — no per-request overhead.</li><li>Protocol data is loaded once and rendered once. The custom protocol handler serves pre-rendered HTML from memory.</li><li>Client-side hydration runs identically to the browser — same JS bundle, same FAST-HTML components.</li></ul>`,19)])])}const k=a(n,[["render",o]]);export{u as __pageData,k as default};

{
  "name": "contact-book-manager",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start:client": "esbuild src/index.ts --bundle --outfile=dist/index.js --format=esm --sourcemap --watch",
    "start:server": "cargo run -p webui-cli -- serve ./src --state ./data/state.json --plugin=fast --servedir ./dist --port 3003 --api-port 3013 --watch",
    "start:api": "esbuild server/api.ts --bundle --platform=node --format=esm --outfile=dist/api.js --packages=external && node dist/api.js",
    "start": "cargo xtask dev contact-book-manager",
    "test": "playwright test",
    "test:update-snapshots": "playwright test --update-snapshots"
   },
   "imports": {
    "#atoms/*": "./src/atoms/*",
    "#molecules/*": "./src/molecules/*",
    "#organisms/*": "./src/organisms/*",
    "#pages/*": "./src/pages/*",
    "#app/*": "./src/cb-app/*",
    "#api": "./src/api.ts"
   },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@microsoft/fast-element": "catalog:",
    "@microsoft/fast-html": "catalog:",
    "@microsoft/webui-router": "workspace:*",
    "@playwright/test": "catalog:",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "esbuild": "catalog:",
    "tslib": "catalog:",
    "typescript": "catalog:"
  }
}

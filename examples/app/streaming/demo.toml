# Demo shell metadata — auto-discovered by the demo shell server.
# See examples/demo/README.md for the full schema.

name = "Streaming"
description = "Progressive streaming hydration: priority-ordered composer/weather/feed rendering over one open response"
backend = "node"

[server]
type = "webui-cli"
plugin = "webui"
source = "src"
servedir = "dist"
theme = "@microsoft/webui-examples-theme"
css = "style"
projection-manifest = ["dist/webui-projection.json"]

[api]
type = "node"
entry = "server/dist/index.js"
port-offset = 10

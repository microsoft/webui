# Rust build artifacts
target/

# Node.js dependencies (reinstalled during build)
**/node_modules/

# Git history
.git/
.gitignore

# Test artifacts
**/test-results/

# Release staging
publish/

# Documentation site is part of the pnpm workspace and must remain in the
# Docker build context so frozen-lockfile installs see a complete workspace.
# docs/

# .NET bindings (not needed for commerce build)
dotnet/

# CI configuration
.github/

# IDE and editor files
*.code-workspace
.vscode/
.idea/
